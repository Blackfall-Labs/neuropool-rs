[package]
name = "neuropool"
version = "0.1.0"
edition = "2021"
description = "Biological neuron pool substrate — LIF neurons with CSR synapses, eligibility traces, and three-factor plasticity"
license = "MIT OR Apache-2.0"
keywords = ["neuromorphic", "spiking", "plasticity", "neuron", "synapse"]
categories = ["science", "simulation"]

[dependencies]
ternary-signal = { path = "../ternary-signal" }
log = "0.4"

[dev-dependencies]
env_logger = "0.11"
